{# image is above content for mobile screens and to left otherwise #}
<article class="card-row">
  <div class="card-rcontainer">
    {% if image %}
      <div>
        {{ image}}
      </div>
    {% endif %}
    <div class="card-content">
     <h2 class="card-title"><a href="{{ url }}">{{ heading }}</a></h2>
      {% if date or location %}
        {{ date }}
        {{ location }}
      {% endif %}
    </div>
    <div class="card-content">
      {{ content }}
    </div>
  </div>
</article>
